<% provide :title, t(".title") %>
<h1><%= t ".title" %></h1>

<%= render partial: "orders/status_list", locals: {orders: @orders, current_status: @current_status} %>

<div id="order_list">
  <turbo-stream action="replace" target="order_list">
    <template>
      <div class="container-fluid">
        <div class="row">
          <div class="col">
            <table class="table table-striped table-hover order-list">
              <%= render partial: "orders/orders_header" %>
              <tbody>
                <%= render partial: "orders/orders_list", collection: @orders, as: :order %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </template>
  </turbo-stream>
</div>
